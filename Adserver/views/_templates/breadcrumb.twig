{% if breadcrumb %}
	<ol class="breadcrumb">
		{% for label,urlz in breadcrumb %}
			<li {% if loop.last %}class="active"{% endif %}>
				<a href="{{ urlz }}">{{ label|raw }}</a>
			</li>
		{% endfor %}	
	</ol>
{%  endif %}